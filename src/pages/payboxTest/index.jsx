import cn from "classnames";
import css from "./styles.module.scss"

export const PayboxTest = () => {
    return  (
        <div className={cn("container", css.root)}>
            Paybox
        </div>
    );
};